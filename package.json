{
  "name": "imsonia-search",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node start",
    "test": "export NODE_ENV=test || SET NODE_ENV=test && mocha --opts test/mocha.opts test/**/**/*.js",
    "test:unit": "export NODE_ENV=test || SET NODE_ENV=test && mocha --opts test/mocha.opts test/unit/**/*.js",
    "test:integration": "export NODE_ENV=test || SET NODE_ENV=test && mocha --opts test/mocha.opts test/integration/**/*.js"
  },
  "author": "Lucas Andrade",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "dotenv": "^5.0.0",
    "express": "^4.16.2",
    "helmet": "^3.11.0",
    "mongoose": "^5.0.5"
  },
  "devDependencies": {
    "expect": "^22.3.0",
    "mocha": "^5.0.1"
  }
}
